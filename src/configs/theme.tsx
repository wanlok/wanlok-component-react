import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#FF0000",
      //   light: "#0000FF",
      dark: "#00FF00",
      contrastText: "#0000FF"
    }
  }
});
