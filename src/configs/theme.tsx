import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#DDDDDD",
      dark: "#CCCCCC",
      contrastText: "#000000"
    },
    background: {
      default: "#EEEEEE",
      paper: "#FFFFFF"
    },
    divider: "#CCCCCC"
  }
});
