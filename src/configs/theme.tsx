import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#FF0000",
      //   light: "#0000FF",
      dark: "#FF00FF",
      contrastText: "#0000FF"
    }
  }
});
