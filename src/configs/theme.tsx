import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#DDDDDD",
      dark: "#CCCCCC",
      contrastText: "#000000"
    },
    background: {
      paper: "#FFFFFF"
    }
  }
});
